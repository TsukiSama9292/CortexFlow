{
  "name": "@cortexflow/cli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run cortexflow --topic 'AI Agents' --demo",
    "build": "echo 'No build step required for Python CLI'",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format .",
    "type-check": "uv run pyright",
    "test": "uv run pytest"
  }
}
